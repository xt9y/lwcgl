v3.4.2 stabilization - 2026-09-05

- context-affine thread-local OpenGL dispatch with transactional capability creation
- stale capability rejection after context destruction/reuse
- reusable thread-local MemoryStack arena and corrected memLengthNT1 semantics
- selected high-throughput upload, MRT, buffer-range, shader-binary, compute and readback APIs
- GLFW 3.3 minimum with monitor/video-mode, cursor, gamepad and error operations
- collision-safe canonical public types/constants plus opt-in compatibility aliases
- versioned install paths, pkg-config metadata, build configuration fingerprinting and stale-archive prevention
- executable context, memory, header-order, staged-install, runtime and sanitizer tests
- manual phase-gate CI and explicit source export
