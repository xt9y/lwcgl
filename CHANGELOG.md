# Changelog

## 2.9.3 hardening

- bounded and typed legacy buffers
- transactional Display fullscreen/lifecycle state
- framebuffer-pixel dimensions
- deterministic input queues with overflow counters
- fractional mouse delta preservation
- complete LWJGL key naming table and repeat-character association
- absolute-deadline frame pacing
- progressive per-thread modern OpenGL loading
- thread-local error state
- immutable versioned API tables
- versioned install/pkg-config artifacts
- executable runtime, header-order, staged-consumer and sanitizer CI coverage
